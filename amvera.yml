# Amvera Cloud: деплой из Docker (фронт + бэкенд в одном образе)
# Документация: https://docs.amverum.com/
meta:
  environment: docker
  toolchain:
    name: docker
    version: "20"

run:
  containerPort: "8000"
